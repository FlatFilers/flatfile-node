/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as serializers from "../../../index";
import * as Flatfile from "../../../../api/index";
import * as core from "../../../../core";
import { UserCredentials } from "./UserCredentials";
import { ApiCredentials } from "./ApiCredentials";

export const Credentials: core.serialization.Schema<serializers.Credentials.Raw, Flatfile.Credentials> =
    core.serialization
        .union("type", {
            userCredentials: UserCredentials,
            apiCredentials: ApiCredentials,
        })
        .transform<Flatfile.Credentials>({
            transform: (value) => value,
            untransform: (value) => value,
        });

export declare namespace Credentials {
    export type Raw = Credentials.UserCredentials | Credentials.ApiCredentials;

    export interface UserCredentials extends UserCredentials.Raw {
        type: "userCredentials";
    }

    export interface ApiCredentials extends ApiCredentials.Raw {
        type: "apiCredentials";
    }
}
